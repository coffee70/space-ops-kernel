unit_id: telemetry-query-service
display_name: Telemetry Query Service
package_owner: space-ops-platform
runtime_kind: service
runtime_template: python-service
source_path: project/space-ops-platform
build:
  command: pip install -r requirements.txt
run:
  command: sh -c "cd /app/platform/backend && uvicorn main:app --app-dir services/telemetry-query-service --host 0.0.0.0 --port 8080"
health:
  type: http
  path: /health
  port: 8080
discovery:
  service_slug: telemetry-query-service
  category: telemetry
  api_base_path: /telemetry
  capability_tags:
    - query
    - dashboard
    - watchlist
  health_endpoint: /health
  visibility: internal
  depends_on:
    - source-registry-service
    - telemetry-ingest-service
